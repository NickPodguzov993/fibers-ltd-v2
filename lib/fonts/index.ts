import { Inter, Rubik } from "next/font/google";

export const titleFont = Inter({ weight: ["400", "700"], subsets: ["latin"] });
export const textFont = Rubik({
  weight: ["400", "500", "700"],
  subsets: ["latin"],
});
